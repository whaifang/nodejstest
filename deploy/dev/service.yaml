kind: Service
apiVersion: v1
metadata:
  name: nodejs5
  namespace: demo
spec:
  ports:
    - name: http-port
      protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30675
  selector:
    app: nodejs5